1. ѕодготовка (Ћокально)
≈сли вы только создали папку с кодом:

git init Ч превращает обычную папку в Git-репозиторий (создает скрытую папку .git).

git status Ч показывает состо€ние файлов: что изменилось, что будет добавлено в коммит, а что Git Ђне видитї.

2. ќчистка и игнорирование (¬аш случай)
≈сли вы случайно добавили лишние файлы (bin, obj) и исправили .gitignore:

git rm -r --cached . Ч Ђзабытьї все файлы в текущей папке. —ами файлы остаютс€ на диске, но Git перестает их отслеживать.

git add . Ч добавить все файлы заново. Ќа этом этапе Git загл€нет в ваш исправленный .gitignore и пропустит ненужные папки.

git status Ч проверка: вы должны увидеть список файлов со статусом deleted (те, что ушли в игнор).

3. —охранение и отправка
git commit -m "сообщение" Ч создает Ђснимокї (версию) вашего кода с кратким описанием изменений.

git branch -M main Ч переименовывает основную ветку в main (современный стандарт GitHub).

git remote add origin <ссылка> Ч св€зывает ваш компьютер с репозиторием на сервере GitHub.

git push -u origin main Ч отправл€ет ваши коммиты в облако. ‘лаг -u запоминает настройки, чтобы в следующий раз писать просто git push.

4. –абота в процессе
 огда вы написали новый код и хотите его отправить:

git add . (подготовить изменени€).

git commit -m "решил задачу є5" (сохранить локально).

git push (отправить на GitHub).

5. »зменение файла
ren .gitignore.txt .gitignore